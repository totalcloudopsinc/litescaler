folder_id  = "b1gs6pmsqltug8ii0j5d"
# One Managed Kubernetes cluster serves both dev and prod; the environments are
# separated by namespace (bot-dev / bot-prod), not by cluster.
cluster_id = "catp68de0kgi9fc22v0c"
env        = "dev"
namespace  = "kube-system"
